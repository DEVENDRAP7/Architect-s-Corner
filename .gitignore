# Python
__pycache__/
*.pyc
.venv/
venv/

# PyInstaller build output (the .exe is built by CI / locally, not committed)
build/
dist/

# Runtime data (user uploads + manifest live in Documents\ArchitectsCorner)
uploads/
logs/
project.json

# Confidential drawings / spreadsheets — never commit
*.dxf
*.dwg
*.xls
*.xlsx

# Local assistant / session folders
.claude/
.remember/

# Landing-site runtime data (analytics counters, regenerated sample)
site/site_stats.json
site/site_messages.json
site/site_admin.json
site/OWNER-GUIDE.md

# Dead AI harness + internal dev notes (not part of the product)
ask.py
context.md

# OS junk
Thumbs.db
desktop.ini
.DS_Store

# BOQ comparer runtime uploads
boq/uploads/
test_quote*
quote_p*.png
quote_scanned.pdf

rates.json

# Artefacts written by test_all_tools.py when it exercises the export tools
test_building_entities.csv
test_building_text.txt

